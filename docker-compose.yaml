services:
  backend:
    build: ./backend
    environment:
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis
    ports:
      - "127.0.0.1:8000:8000"

  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
